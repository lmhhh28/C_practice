#include <stdio.h>

struct Node {
    int data;
    struct Node* next;
};
typedef struct Node Node;
typedef Node* List;

void Input(List list, int n) {
    for(int i = 0; i < n; ++i) {
        
    }
}
int main() {

}